name: lunya
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=2.19.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  # Проверьте эти строки:
  http: ^1.1.0
  async_wallpaper: ^2.0.3
  path_provider: ^2.1.1
  url_launcher: ^6.2.1  # <-- ЭТО ВАЖНО, без этого код не соберется
  
  # Для иконок (обычно есть по умолчанию)
  cupertino_icons: ^1.0.2


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
